/**
 * @file array_string.h
 * @brief 数组与字符串模块头文件
 * @details 涵盖一维数组、多维数组、字符数组、字符串操作等知识点。
 *          数组是C语言中最基本的数据结构，字符串处理是嵌入式开发的核心技能。
 * 
 * @section main_features 主要内容
 * - 一维数组：声明、初始化、访问、遍历
 * - 多维数组：二维数组、三维数组、内存布局
 * - 字符数组与字符串：字符串本质、初始化方式
 * - 字符串操作：strlen、strcpy、strcat、strcmp等
 * - 字符串处理技巧：分割、转换、修剪、格式化
 * - 数组排序：冒泡、选择、插入排序
 * - 数组查找：线性查找、二分查找
 * - 常见错误：越界、溢出、未初始化
 * 
 * @section embedded_importance 嵌入式开发重要性
 * - 数组是数据存储的基础结构
 * - 字符串处理用于通信协议解析
 * - 缓冲区管理是嵌入式开发的核心技能
 * - 数组越界是常见的内存错误来源
 * 
 * @section best_practices 最佳实践
 * - 使用ARRAY_SIZE宏计算数组元素个数
 * - 字符串操作前检查缓冲区大小
 * - 使用strncpy等安全函数
 * - 确保字符串以'\0'结尾
 * 
 * @author Embedded C Learning Project
 * @date 2026-03-22
 * @version 1.0.0
 * 
 * @note 本文件遵循 MISRA-C:2012 编码规范
 */

#ifndef ARRAY_STRING_H
#define ARRAY_STRING_H

#ifdef __cplusplus
extern "C" {
#endif

#include "common/common_types.h"

/*============================================================================*/
/*                           一维数组演示                                       */
/*============================================================================*/

/**
 * @brief 一维数组演示
 * @details 展示一维数组的基本操作：
 *          - 数组声明与初始化
 *          - 数组元素的访问
 *          - 数组大小计算
 *          - 数组遍历
 */
void demo_one_dimensional_array(void);

/*============================================================================*/
/*                           多维数组演示                                       */
/*============================================================================*/

/**
 * @brief 多维数组演示
 * @details 展示多维数组的使用：
 *          - 二维数组声明与初始化
 *          - 二维数组访问
 *          - 三维数组示例
 *          - 内存布局
 */
void demo_multi_dimensional_array(void);

/*============================================================================*/
/*                           字符数组与字符串演示                               */
/*============================================================================*/

/**
 * @brief 字符数组与字符串演示
 * @details 展示字符数组与字符串的关系：
 *          - 字符数组声明与初始化
 *          - 字符串的本质（以'\0'结尾的字符数组）
 *          - 字符串字面量
 *          - 字符数组与字符串的区别
 */
void demo_character_array_string(void);

/*============================================================================*/
/*                           字符串操作函数演示                                 */
/*============================================================================*/

/**
 * @brief 字符串操作函数演示
 * @details 展示常用字符串操作函数：
 *          - strlen: 字符串长度
 *          - strcpy/strncpy: 字符串复制
 *          - strcat/strncat: 字符串连接
 *          - strcmp/strncmp: 字符串比较
 *          - strchr/strrchr: 字符查找
 *          - strstr: 子串查找
 */
void demo_string_operations(void);

/*============================================================================*/
/*                           字符串处理技巧演示                                 */
/*============================================================================*/

/**
 * @brief 字符串处理技巧演示
 * @details 展示实用的字符串处理技巧：
 *          - 字符串分割
 *          - 字符串转换
 *          - 字符串修剪
 *          - 字符串格式化
 */
void demo_string_techniques(void);

/*============================================================================*/
/*                           数组排序演示                                       */
/*============================================================================*/

/**
 * @brief 数组排序演示
 * @details 展示常用的数组排序算法：
 *          - 冒泡排序
 *          - 选择排序
 *          - 插入排序
 *          - 快速排序
 */
void demo_array_sorting(void);

/**
 * @brief 冒泡排序
 * @param[in,out] arr 待排序数组
 * @param[in] size 数组大小
 */
void bubble_sort(int32_t arr[], size_t size);

/**
 * @brief 选择排序
 * @param[in,out] arr 待排序数组
 * @param[in] size 数组大小
 */
void selection_sort(int32_t arr[], size_t size);

/**
 * @brief 插入排序
 * @param[in,out] arr 待排序数组
 * @param[in] size 数组大小
 */
void insertion_sort(int32_t arr[], size_t size);

/**
 * @brief 打印数组
 * @param[in] arr 数组指针
 * @param[in] size 数组大小
 */
void print_array(const int32_t arr[], size_t size);

/*============================================================================*/
/*                           数组查找演示                                       */
/*============================================================================*/

/**
 * @brief 数组查找演示
 * @details 展示常用的数组查找算法：
 *          - 线性查找
 *          - 二分查找
 *          - 查找算法比较
 */
void demo_array_searching(void);

/**
 * @brief 线性查找
 * @param[in] arr 数组指针
 * @param[in] size 数组大小
 * @param[in] target 目标值
 * @return 找到返回索引，未找到返回-1
 */
int32_t linear_search(const int32_t arr[], size_t size, int32_t target);

/**
 * @brief 二分查找
 * @param[in] arr 已排序数组指针
 * @param[in] size 数组大小
 * @param[in] target 目标值
 * @return 找到返回索引，未找到返回-1
 */
int32_t binary_search(const int32_t arr[], size_t size, int32_t target);

/*============================================================================*/
/*                           数组常见错误演示                                   */
/*============================================================================*/

/**
 * @brief 数组常见错误演示
 * @details 展示数组使用中的常见错误：
 *          - 数组越界
 *          - 缓冲区溢出
 *          - 未初始化数组
 *          - 字符串未终止
 */
void demo_array_common_errors(void);

#ifdef __cplusplus
}
#endif

#endif
